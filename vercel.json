{
  "installCommand": "npm exec --yes --package=pnpm@10.25.0 -- pnpm install --frozen-lockfile",
  "buildCommand": "npm exec --yes --package=pnpm@10.25.0 -- pnpm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
