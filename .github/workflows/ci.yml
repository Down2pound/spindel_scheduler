name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm exec --yes --package=pnpm@11.9.0 -- pnpm install --frozen-lockfile

      - name: Run parser regression tests
        run: npm exec --yes --package=pnpm@11.9.0 -- pnpm run test:parser

      - name: Type-check
        run: npm exec --yes --package=pnpm@11.9.0 -- pnpm run lint

      - name: Build
        run: npm exec --yes --package=pnpm@11.9.0 -- pnpm run build
